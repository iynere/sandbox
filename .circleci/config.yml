version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout

      - run: |
          git clone git@github.com:CircleCI-Public/circleci-dockerfiles.git
          mkdir test
          touch test/foo
          touch test/bar
          cp -rf ~/project/test/* circleci-dockerfiles
          cd circleci-dockerfiles && git checkout -b test && git add . && git commit -m 'testing' && git push origin test